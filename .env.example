# MeanMessenger Backend Environment Variables
# Copy this to .env and update with your actual values

# Server Configuration
PORT=8000
NODE_ENV=development

# Database Configuration
MONGO_DB_URI=mongodb+srv://username:password@cluster.mongodb.net
CORS_ORIGIN=http://localhost:3000
CLIENT_URL=*

# JWT Configuration
ACCESS_TOKEN_SECRET="your-super-secret-access-token-key"
ACCESS_TOKEN_EXPIRY=1800000
REFRESH_TOKEN_SECRET="your-super-secret-refresh-token-key" 
REFRESH_TOKEN_EXPIRY=604800000

# Password Encryption
BCRYPT_SALT_ROUNDS=12

# SMTP Email Configuration (Required for Forgot Password)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-specific-password"
SMTP_FROM="MeanMessenger <your-email@gmail.com>"

# Cloudinary Configuration (for file uploads)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
CLOUDINARY_URL=cloudinary://api-key:api-secret@cloud-name

# ==========================================
# SMTP Setup Instructions:
# ==========================================
# 
# For Gmail:
# 1. Enable 2-Factor Authentication
# 2. Go to Google Account Settings > Security > 2-Step Verification > App passwords
# 3. Generate app password for "Mail"
# 4. Use the generated password in SMTP_PASS
# 5. Use your Gmail address in SMTP_USER
#
# For other providers:
# - Outlook: smtp-mail.outlook.com:587
# - Yahoo: smtp.mail.yahoo.com:587
# - SendGrid: smtp.sendgrid.net:587
#
# ==========================================
# Testing Instructions:
# ==========================================
#
# 1. Update SMTP_USER with your email
# 2. Start the server: npm start
# 3. Test endpoints:
#    - POST /api/users/forgot-password/request
#    - POST /api/users/forgot-password/verify  
#    - POST /api/users/forgot-password/reset
#
# 4. Or run test script: node test-forgot-password.js
#
# ==========================================